// const webPush = require('web-push');

// const list = []

// webPush.setVapidDetails(process.env.PUBLIC_IP, process.env.VAPID_PUBLIC, process.env.VAPID_PRIVATE);

// exports.subscribe = (data) => {list.push(data)}

// exports.send = (data) => {
//     list.forEach(e => {
//         console.log(data)
//         webPush.sendNotification(e, data)
//         .catch(error => console.error(error));
//     });
// }